## Import Narrative Text

Run from the `backend` directory:

```bash
python -m app.services.import_narrative_text
```

## Import References

Run from the `backend` directory:

```bash
python -m app.services.import_references
```
